{% for interface in device.interfaces.all %}
    {% if 'internet' in interface.tags.all | map(attribute='name') %}
        Interface: {{ interface.name }}
        Tags: 
        {% for tag in interface.tags.all %}
            {{ tag.name }}
        {% endfor %}
        ---
    {% endif %}
{% endfor %}
